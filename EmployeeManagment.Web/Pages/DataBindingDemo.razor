@page "/data-binding-demo"

@inherits DataBindingDemoBase

<h1>Data Binding Demo</h1>

<hr />

<h3>One Way Data Binding Data flow from compontent c# code(class) to the view</h3>


<div>
    <b>Name:</b> @Name
</div>

<div>
    <b>Name With salution:</b> @(Gender == "Male"? $"Mr. {Name}" : $"Miss. {Name}")
</div>

<div>
    <b>Name:</b>  <input value=@Name />
</div>

<hr />
<h3>Two Way Data Binding Data flow from component c# code(class) to the view and vice versa</h3>

<div>
    <b>Name:</b>  <input value=@Name @onchange="@(e => { Name = e.Value.ToString(); })" />
</div>
<hr />
<h4>Simpler way for Two Way Data Binding Data by using Bind attribute only name updated after the input lose its focus by pressing tab</h4>
<div>
    <b>Name:</b>  <input @bind="Name" />
</div>
<hr />
<h4>name change by typing inside the input as we specify the event to be oninput</h4>
<div>
    <b>Name:</b>  <input @bind="Name" @bind:event="oninput"/>
</div>

<hr />
<h4> bind html element attribute</h4>
<div>
    <b>Colour:</b>  <input @bind="Colour"/>
    <div @bind-style="Colour" @bind-style:event="onchange">
        this is the backgroud color you type
    </div>
</div>